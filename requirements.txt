# Core ML and Data Science
# NOTE: TensorFlow is intentionally omitted here to avoid OS-specific wheel
# issues (macOS / Apple Silicon). Install TensorFlow separately following
# the platform-specific instructions in `docs/TENSORFLOW_SETUP.md`.
pandas>=1.3.0
# Keep NumPy under 2.0 for compatibility with older TF builds
numpy<2
scikit-learn>=1.0.0
joblib>=1.1.0

# Yahoo Finance Integration
yfinance>=0.2.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Additional useful packages
requests>=2.25.0
python-dateutil>=2.8.0
schedule>=1.2.0
